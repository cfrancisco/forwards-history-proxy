// const powerDemandCalc = require('../src/powerDemandCalc');
/*
it('should calc power demand (no rush time)', () => {
  const historicalData = {
    activePowerABC: [
      { device_id: 'meter1', value: 10.5 },
      { device_id: 'meter1', value: 20.8 },
      { device_id: 'meter1', value: 11.5 },
      { device_id: 'meter1', value: 31.2 },
    ],
  };

  const historicalDataList = [historicalData];
  const powerDemand = powerDemandCalc.calc(historicalDataList);

  expect(
    powerDemand[0].attrs[powerDemandCalc.POWER_DEMAND_NORMAL_TIME],
  ).toEqual(18.5);
});

*/
